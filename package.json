{
  "name": "workbox-app",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "dhost --port $PORT .",
    "build": "esbuild --outfile=sw.js --bundle sw.ts && workbox injectManifest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dhost": "^0.3.5"
  },
  "devDependencies": {
    "esbuild": "^0.19.5",
    "workbox-cli": "^7.0.0",
    "workbox-precaching": "^7.0.0",
    "workbox-routing": "^7.0.0",
    "workbox-strategies": "^7.0.0"
  }
}
